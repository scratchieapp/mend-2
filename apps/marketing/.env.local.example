# Marketing Website Environment Variables
# Copy this to .env.local and fill in your values

# Google Analytics 4 - Get from GA Admin → Data Streams
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Clerk Authentication (same as operations app)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_key_here

# Development URLs
VITE_OPERATIONS_URL=http://localhost:5173/operations
VITE_PUBLIC_URL=http://localhost:5174

# Production URLs - Enable these for production deployment
# Main marketing site runs on mendplatform.au
# Operations/authentication runs on accounts.mendplatform.au subdomain
# VITE_OPERATIONS_URL=https://accounts.mendplatform.au
# VITE_PUBLIC_URL=https://mendplatform.au

# URL Configuration Guide:
# - Development: operations runs on localhost:5173/operations
# - Production: operations runs on accounts.mendplatform.au subdomain
# - Marketing login/signup buttons redirect to the operations domain
# - Clerk should be configured with accounts.mendplatform.au as allowed origin
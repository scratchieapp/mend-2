project_id = "rkzcybthcszeusrohbtc"

# Third-Party Auth integration with Clerk
# This allows Clerk JWTs to be used directly without going through GoTrue
[auth.third_party.clerk]
enabled = true
# Your Clerk domain (without https://)
domain = "clerk.mendplatform.au"

# Edge Function configurations
# Disable JWT verification for functions that need to be called from browsers
# without requiring Supabase Auth (using Clerk instead)
[functions.create-web-call]
verify_jwt = false